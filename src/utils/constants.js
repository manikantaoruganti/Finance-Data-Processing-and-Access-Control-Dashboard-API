/**
 * Defines constants for user roles.
 */
const USER_ROLES = {
  VIEWER: 'viewer',
  ANALYST: 'analyst',
  ADMIN: 'admin',
};

/**
 * Defines constants for user statuses.
 */
const USER_STATUSES = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
};

/**
 * Defines constants for financial record types.
 */
const RECORD_TYPES = {
  INCOME: 'income',
  EXPENSE: 'expense',
};

module.exports = {
  USER_ROLES,
  USER_STATUSES,
  RECORD_TYPES,
};
